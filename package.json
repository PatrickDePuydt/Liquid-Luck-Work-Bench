{
  "name": "Liquid-Luck-Work-Bench",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "postcss 'src/main.css' --output 'dist/main.css' --use postcss-cssnext",
    "build:watch": "npm run build -- --watch",
    "server": "lr-http-server",
    "start": "npm-run-all -p build:watch server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "postcss-cli": "^4.1.1",
    "postcss-cssnext": "^3.0.2"
  },
  "devDependencies": {
    "lr-http-server": "^0.1.5",
    "npm-run-all": "^4.1.1"
  }
}
